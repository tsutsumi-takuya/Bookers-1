<h1>New book</h1>
	<!-- form_for部分 -->
<%= form_for(@book) do |f| %>

	<%= render 'layouts/error', book: @book %>

	<label for="title">
		Title
	</label>
	<%= f.text_field :title %>

	<label for="body">
		Body
	</label>
	<%= f.text_area :body %>

	</br>
	<%= f.submit 'Create Book' %>

<% end %>